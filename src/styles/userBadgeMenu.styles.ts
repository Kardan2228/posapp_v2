import { StyleSheet } from 'react-native';

export const stylesBadge = StyleSheet.create({
    avatarContainer: {
        marginRight: 10,
    },
    appbarTitle: {
        fontWeight: 'bold',
        fontSize: 25,
        textAlign: 'center',
    },
});
